import Layout from "./components/layout/Layout";

function App() {
  return (
    <Layout onBackClick={() => {}} handleSearch={() => {}}>
      Place content here
    </Layout>
  );
}

export default App;
